class Bookmark {
    constructor(id, title, url, source) {
        this.id     = id;
        this.title  = title;
        this.url    = url;
        this.source = source;
    }
}

module.exports.Bookmark = Bookmark;
